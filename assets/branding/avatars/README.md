# MTA Webview Avatar

Place the MTA avatar here as `avatar.svg`.

Requirements:
- Filename: `avatar.svg`
- Format: SVG
- Purpose: Shows in webview UI components

This avatar will be copied to `webview-ui/public/avatarIcons/avatar.svg` during the branding process.